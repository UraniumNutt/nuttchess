use crate::board::*;
use crate::generate::*;
use crate::tables::*;

// Constants
const white_win: isize = 10000;
const black_win: isize = -10000;
const draw: isize = 0;

// Returns a score for the given board position
pub fn eval(board: &BoardState) -> isize {
    todo!();
}
